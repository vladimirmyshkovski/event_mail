============
Installation
============

At the command line::

    $ easy_install event-email

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv event-email
    $ pip install event-email
